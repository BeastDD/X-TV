{
  "name": "types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types (Channel, User, TweetMetadata, etc.)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'types package placeholder'"
  }
}
