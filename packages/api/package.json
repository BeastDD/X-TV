{
  "name": "@repo/api",
  "version": "0.1.0",
  "private": true,
  "description": "Shared API clients, X API wrappers, Supabase helpers (populated in S1+)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'api package placeholder (real X client + curation jobs in Sprint 1)'"
  },
  "dependencies": {},
  "devDependencies": {}
}
